server:
  port: 8083

spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ms_notification
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

# Configura??es do Swagger
springdoc:
  pathsToMatch: /**
  swagger-ui:
    user-root-path: true
